<%- include("./partials/head") %>
<html>
    <body id="body-index">
        <%- include("./partials/nav") %>
        <div class="main">
                <div class="search-div">
                    <form action="/" method="GET" class="flex">  <!-- Form to submit the search query -->
                        <input type="search" name="search" id="search" placeholder="Søk gjennom guider med tittel (case sensitive)" autocomplete="off" value="<%= searchQuery %>">
                        <button type="submit" id="search-button">SØK</button>
                    </form>
                </div>
            <div class="main-content">
                <% if (guides.length > 0) { %>
                    <% guides.forEach(guide => { %>
                        <a href="/guide/<%= guide._id %>">
                            <div class="guide-link"><%= guide.tittel %></div>
                        </a>
                    <% }); %>
                <% } else { %>
                    <p>Ingen guider funnet.</p>
                <% } %>
            </div>
        </div>
    </body>
</html>
